<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
		HalWatchComplication — WidgetKit extension Info.plist.

		Required keys for a watchOS WidgetKit extension. The principal class
		is left empty because modern WidgetKit (iOS 14+ / watchOS 7+) uses
		the @main attribute on a WidgetBundle struct instead of an Objective-C
		principal class. The NSExtensionPointIdentifier is the WidgetKit
		identifier, same for iOS and watchOS extension targets.

		When Mark wires up the target in Xcode, this plist may be regenerated
		by the New Target wizard — replace it with this version (or leave
		the wizard's version since the wizard knows what it's doing for the
		current SDK).
	-->
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Hal</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
</dict>
</plist>
